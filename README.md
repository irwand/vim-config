# vim-config
irwand vim config

Installation:
* install git
* on Windows: ~$ git clone https://github.com/irwand/vim-config.git vimfiles
* on UNIX:    ~$ git clone https://github.com/irwand/vim-config.git .vim
* launch vim and install plugins with :PlugInstall
